spring:
  application:
    name: client


eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    preferIpAddress: true
ribbon:
  http:
    client:
      enabled: true

server:
  port: 8087
  servlet:
    context-path: /client/